class Person:
    def __init__(self, state):
        origin = state.buildings[]
        destination = state.buildings[]
        while destination.name == origin.name:
            destination = state.buildings


### need to build random number generators the same way as Arnaud
    @staticmethod
    def random_integer(min, max):
        round((max-min)*random_number_generator.next() + min)

    @staticmethod
    def random